create virtual environment .venv
install libs into .venv env

app.py - single thread and multithread example
app_asyn_aio.py - asyncio and aiohttp example

You can run each script separated and see results in terminal. 